<h1><%= t('views.common.title_index', name: @users.model_name.human) %></h1>

<ul class="user_list">
<% @users.each do |user| %>
  <li><%= link_to user.name, user %></li>
<% end %>
</ul>

<%= paginate @users %>

<p><%= link_to t('views.button.to_books'), books_path %></p>
